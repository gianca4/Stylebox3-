services:
  - type: web
    name: stylebox
    env: php
    buildCommand: composer install --no-dev && php artisan config:cache
    startCommand: php artisan serve --host=0.0.0.0 --port=$PORT
```

Guárdalo como `render.yaml` en `C:\xampp\htdocs\Stylebox3-main\` y luego súbelo a GitHub:
```
git add .
git commit -m "add render.yaml"
git push -u origin master